import styled from "styled-components";

const MemeContainer = styled.section`
  max-width: 1200px;
  margin: 2rem auto;
  display: flex;
  flex-wrap: wrap;
  gap: 1rem;
`;

export { MemeContainer };
